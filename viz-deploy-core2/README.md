# Big Five Steering — core asset (chapters 01–02)

Standalone companion viz for sharing with code. Two chapters only:

1. **Embedding + MPI-120** (`big_five_tsne.html`) — persona as L15 activation points + inventory measurement graph  
2. **Five personas** (`big_five_persona.html`) — monotonic α·v̂ steering on the Big Five dial

Full seven-chapter series: https://viz-deploy-blue.vercel.app/

## Local

Open `viz_series.html` in a browser (static files; no build).

## Deploy

```bash
cd viz-deploy-core2
vercel --prod --yes
```
